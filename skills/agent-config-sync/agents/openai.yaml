interface:
  display_name: "Agent Config Sync"
  short_description: "从现有配置抽取事实并安全同步 Codex 与 Claude Code"
  default_prompt: "使用 $agent-config-sync 归并现有 Codex 与 Claude Code 配置，通过本地界面审阅冲突和配置说明后生成并验证 agent-config.yaml。"
