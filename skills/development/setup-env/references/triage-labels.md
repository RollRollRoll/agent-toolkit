# Triage 标签

这套 skill 用**五个标准 triage 角色**说话。这份文件把那些角色映射到这个仓库 issue tracker 里
**实际在用的标签字符串**。

| agent-toolkit 里的标签 | 我们 tracker 里的标签 | 含义 |
| --- | --- | --- |
| `needs-triage` | `needs-triage` | 维护者需要评估这个 issue |
| `needs-info` | `needs-info` | 等报告者补更多信息 |
| `ready-for-agent` | `ready-for-agent` | 规格已完整，可以交给 AFK agent |
| `ready-for-human` | `ready-for-human` | 需要人来实现 |
| `wontfix` | `wontfix` | 不会去处理 |

**某个 skill 提到一个角色时**（比如「打上 AFK-ready 那个 triage 标签」），
**用这张表里对应的那个标签字符串**。

**右边那列随你改**，改成你实际在用的词汇。
