interface:
  display_name: "Create Questionnaire"
  short_description: "为需要向知情人确认的问题生成 Markdown 问卷。"
  default_prompt: "使用 $create-questionnaire 把这件我无法独立确认的事整理成一份可发给知情人填写的问卷。"
policy:
  allow_implicit_invocation: false
