---
name: setup-worktree
description: 在开始开发或并行任务前建立并验证隔离的 Git worktree。用于开 worktree、隔离工作区或为并行任务建立独立基线；不用于小改动、收尾清理或非 Git 项目。
---

完整读取并严格执行 [Setup Worktree 主体](../../resources/skills/setup-worktree/SKILL.md)。
后续相对引用以该主体文件所在目录为基准解析。本文件只作为 Codex 插件的薄适配入口，不改变 Claude Code 使用的原始 Skill。
